# 写作工坊删除确认设计

写作工坊不再使用浏览器 `window.confirm`。删除写作记录或收藏句子、删除学习总结，以及生成总结后删除源内容，全部通过共享 `ConfirmModal` 二次确认。Modal 显示实际影响数量，取消不改变数据，确认按钮使用危险样式。

写作记录和学习总结工具栏中的删除操作改为 `Trash2` icon button。按钮提供本地化 `title` 和 `aria-label`，没有选中内容时禁用；确认 Modal 内仍使用文字按钮明确最终操作。
